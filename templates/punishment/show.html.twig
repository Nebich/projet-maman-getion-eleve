{% extends 'base.html.twig' %}

{% block title %}Punishment{% endblock %}

{% block body %}
    <h1>Punishment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ punishment.id }}</td>
            </tr>
            <tr>
                <th>Wording</th>
                <td>{{ punishment.wording }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ punishment.date ? punishment.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('punishment_index') }}">back to list</a>

    <a href="{{ path('punishment_edit', {'id': punishment.id}) }}">edit</a>

    {{ include('punishment/_delete_form.html.twig') }}
{% endblock %}
