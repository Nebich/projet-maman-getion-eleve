{% extends 'base.html.twig' %}

{% block title %}Abscence{% endblock %}

{% block body %}
    <h1>Abscence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abscence.id }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ abscence.reason }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ abscence.date ? abscence.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abscence_index') }}">back to list</a>

    <a href="{{ path('abscence_edit', {'id': abscence.id}) }}">edit</a>

    {{ include('abscence/_delete_form.html.twig') }}
{% endblock %}
