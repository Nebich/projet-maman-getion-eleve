{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="btn-group mt-3">
        <a href="{{ path('group_index') }}" class="btn btn-danger"><i class="bi bi-arrow-left"></i> Retour</a>
        <button class="btn btn-primary"> {{ button_label|default('Enregistrer') }}</button>
    <div>
{{ form_end(form) }}
